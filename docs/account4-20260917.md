# lfxwalkerjohn 独立网关（2026-09-17）

用户要求将 Sub2API 的 `lfxwalkerjohn@gmail.com` 导入服务器框架，并明确先走二号静态家宽。原有 account-2 邮箱不同，故新增 account-4，不覆盖既有三个实例。

- main-server：`/opt/codex-official-bridge-accounts/account-4`，容器 `codex-official-bridge-account-4`，监听 `127.0.0.1:8882`。
- 客户端 Base URL：`https://origin-api.monasapi.com/codex/account-4/v1`。网关 Key 仅存该目录私有 `.env`，不记录值。
- nginx：`/etc/nginx/snippets/codex-account4-location.conf`，由既有 `api.monasapi.cn` TLS server include。路径前缀剥除后转发 8882，支持流式和 WebSocket。沿用既有证书，无 DNS 变更。
- 使用既有固定镜像 `sha256:33de4376546dd690bd146d69bf481ea65f44804190c0b1fd7a495407006a5257`；独立运行及认证目录、Key，restart unless-stopped。
- 出口复用 account-3 的 SOCKS 认证及 main-server `127.0.0.1:11082` 隧道，经 BWH `xray-ip2` 到 IP2 `72.234.229.128`。共享 IP2 故障域，未增改 Xray 或 SSH 服务配置。

Sub2API 原账号为 #1 `pro 20x`，原 OAuth 代理为 `TW-static-pro20x`。暂停调度并等待在途请求归零后，将现有 AT、RT、ID Token 导入新实例 `codex-home/auth.json`。启动新实例前将活动数据库账号改为 apikey，移除 OAuth tokens，避免两边继续刷新。保留 ID、名称、分组、并发4及其他原调度参数；proxy_id 清空，由网关管理出站。Sub 的 base_url 为 `https://origin-api.monasapi.com/codex/account-4`，不带末尾 `/v1`。验收后恢复 schedulable=true，并通过 scheduler_outbox 同步 Redis。

验证：四个容器均 running/healthy、重启计数0；新容器同 Rust HTTP 工厂实测出口 IP2；公网无 Key 返回401，有 Key健康200、模型列表200（7模型）；公网 gpt-5.5/low SSE 请求返回 `account4-ok` 且 response.completed；BWH 请求公网模型目录200。未测试长期并发和首次自然 OAuth 刷新。

备份：BWH `/root/codex-bridge-account4-20260917/before.json` 与 `at-transfer.json` 为受保护凭据快照；main-server 同名目录保存 nginx 修改前配置。不能用冷备旧 RT 覆盖未来已刷新的凭据；回退需先暂停、排空、停止网关，再迁回最新 auth。

本地操作脚本和验证结果位于被 Git 忽略的 `.runtime/account-migration/account4.py`、`verify4.py`、`account4-verification.json`。首次部署阶段命令带防重复检查，勿整段盲目重跑。

## 后续更新：Sub2API 新建 Key 条目替换旧条目

用户明确要求删除旧账号条目、新建类似框架测试条目的 Key 账号。已新增 **#13 框架测试-四号实例**：OpenAI/apikey、邮箱 lfxwalkerjohn@gmail.com、Pro、原分组2、并发4、优先级1、倍率1，使用相同 account-4 网关 Base URL 与 Key。extra 仅保留框架透传、Responses、WS off、长上下文计费关闭等配置，不复制旧 OAuth 指纹、自动额度重置及模型冷却历史。

旧 #1 pro 20x 暂停并确认普通/Live在途均0后，移除分组、按软删除方式退役；保留历史用量。新 #13启用调度。scheduler_outbox 同步变更。OAuth继续仅在 main-server account-4持有；公网Base URL、Key和IP2均未改变。替换前受保护备份：BWH `/root/codex-bridge-account4-20260917/sub-new-account/before.json`。
